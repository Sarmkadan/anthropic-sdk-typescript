version: '3.8'

services:
  sdk:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anthropic-sdk
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
